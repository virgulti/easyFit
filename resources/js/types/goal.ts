export type Goal = {
    id: number;
    max_fat_percentage: string | null;
    min_protein_grams: number | null;
    max_calories_per_day: number | null;
    max_calories_per_week: number | null;
};
